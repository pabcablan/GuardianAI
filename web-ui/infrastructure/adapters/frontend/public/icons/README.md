Replace these files with your own icons using the same filenames:

- `brand-mark.svg`
- `sidebar-toggle.svg`
- `add-chat.svg`
- `ai-robot.svg`
- `upload-document.svg`
- `send-message.svg`

You can use SVG, PNG, JPG, or WebP, but if you change the extension you must also update the corresponding `src` paths in the React components.
